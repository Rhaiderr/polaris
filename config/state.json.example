{
  "historyId": null,
  "ultima_execucao": null,
  "_comment": "historyId: Gmail History API cursor (set at bootstrap via getProfile). ultima_execucao: ISO-8601 UTC, used by the messages.list after: fallback when historyId expires. The real file (config/state.json) is gitignored."
}
