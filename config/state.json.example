{
  "historyId": null,
  "ultima_execucao": null,
  "_comentario": "historyId: cursor da History API do Gmail (preenchido no bootstrap via getProfile). ultima_execucao: ISO-8601 UTC, usado no fallback messages.list after: quando o historyId expira. Este arquivo real (config/state.json) é gitignored."
}
