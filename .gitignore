# === Segredos, estado e dados pessoais (NUNCA commitar) ===
# Ignora TUDO em config/ (credentials.json compartilhado + as pastas por-conta
# config/<conta>/ com token.json, categorias.yaml e state.json) — labels reais
# revelam moradia/estudos/investimentos. Versiona só os .example genéricos.
config/*
!config/*.example
.env

# === Logs (contêm remetentes/assuntos reais) ===
logs/

# === Notas locais de contexto (remetentes reais + IPs internos) ===
contexto/

# === Python ===
.venv/
venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# === Docker / diversos ===
*.log
.DS_Store
